    let x = 5;
    let y = ''; 
    function triangle (x,y) {       
        for (let i = 0; i <= x; i++) {
             console.log(y) ;
             y+='*' ;          
            }
        }        
    triangle (x,y);
    return (x,y); 

/*  let x = 5;
    let y = ''; 
    function triangle (x,y) {       
        while (y.length <= x) {
            console.log(y);
            y += '8';

            }
        }        
    triangle (x,y);
    return (x,y);      */